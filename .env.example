# Copia questo file in ".env" e inserisci i tuoi valori.
# Il file .env NON viene versionato.

# Serve SOLO se prompt.mode = "claude" in config.yaml
ANTHROPIC_API_KEY=

# (facoltativo) percorso a un config alternativo
# STITCH_BOT_CONFIG=./config.yaml
